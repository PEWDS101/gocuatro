# The build writes LF. Without this, Windows checkouts rewrite every generated
# page to CRLF and each rebuild shows as a whole-file diff.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary
*.mp4 binary
*.svg text eol=lf
